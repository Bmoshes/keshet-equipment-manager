# הערות מימוש

## מה תועדף

בגלל מגבלת הזמן תועדף ה־Core Flow: צפייה בציוד, הוספת פריט על ידי מנהל, השאלה, "הציוד שלי", החזרה, תמונת מצב למנהל ו־traceability בסיסי באמצעות רשומות השאלה. נוסף גם חיפוש וסינון, מרכז תשומת לב והשבת ציוד על ידי מנהל, משום שהם ממחישים היטב את הערך התפעולי בלי להרחיב את המערכת יתר על המידה.

הלוגיקה העסקית נמצאת ב־`lib/equipment-service.ts`, הנתונים הנגזרים ב־`lib/selectors.ts`, וקומפוננטות ה־UI אינן משנות סטטוסים ישירות. כל הפעולות מחזירות תוצאה מפורשת והודעת משוב בעברית.

## מה לא מומש

- אימות משתמשים והרשאות אמיתיות
- שמירה בבסיס נתונים
- הזמנות עתידיות והתראות
- audit log קבוע או מערכת Events
- QR, עריכת ציוד ומחיקת ציוד
- בדיקות אוטומטיות מלאות

במערכת Production יש להוסיף טרנזקציה או constraint אטומי שמונעים השאלה כפולה, authentication והרשאות בצד השרת, persistence, audit trail ובדיקות יחידה ואינטגרציה לכל מעברי הסטטוס.

## מה הייתי עושה אחרת

1. **שמירה מקומית (LocalStorage):** במקום להסתמך על React State בלבד שמתאפס בעת ריענון דף, הייתי מוסיף שכבת שמירה קלה ב־`localStorage` כ־fallback עבור סביבת הדמו, כדי לשמור על עקביות הנתונים גם במקרה של F5.
2. **הצגת פרטי המחזיק ישירות בכרטיסי המנהל:** הייתי חושף בכרטיס הציוד ברשימה הכללית למנהל את שם העובד שמחזיק בפריט ואת משך ההשאלה, ולא רק בטבלת ההשאלות הפעילות, כדי לאפשר שקיפות מהירה בעת חיפוש או סינון.
3. **בדיקות אוטומטיות ל־State Machine:** הייתי כותב מראש בדיקות יחידה (Unit Tests) מקיפות לכל מעברי הסטטוס והחסימות ב־`equipment-service.ts` (למשל מניעת השאלה כפולה וחסימת מעבר ישיר מ־`borrowed` ל־`unavailable`).

## הנחות

- המשתמש הקבוע במצב עובד הוא בר משה.
- פעולת ההשאלה מייצגת מסירה פיזית מיידית.
- סף "מושאל זמן רב" הוא שבעה ימים, ללא משמעות של איחור.
- רענון הדפדפן מאפס את נתוני הדמו, בהתאם לדרישת שמירה בזיכרון בלבד.

## Prompts מרכזיים

1. "בנה MVP ב־Next.js ו־TypeScript למערכת ניהול ציוד משרדית בעברית וב־RTL, עם הפרדה בין UI, לוגיקה עסקית, נתוני דמו ו־selectors."
2. "ממש flow מלא לעובד ולמנהל: השאלה, החזרה, הרשאות, תמונת מצב, השאלות פעילות ומעברי available / borrowed / unavailable."
3. "לטש את הממשק ככלי עבודה פנימי responsive, הוסף חיפוש, פילטרים, empty states ומשוב הצלחה ושגיאה, והריץ lint ו־build."
