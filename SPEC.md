# אפיון מערכת ניהול והשאלת ציוד

## 1. שאלות מבהירות והנחות עבודה

לצורך התקדמות במסגרת הזמן הנתונה, לכל שאלה נבחרה הנחת עבודה פשוטה שמתאימה ל־MVP. הנחות אלה אינן מנסות לפתור תהליכי Enterprise שלא הוגדרו כצורך.

### 1. האם סוגי ציוד שונים דורשים הרשאות או תהליכים שונים?

לדוגמה, ייתכן שמחשב נייד יקר או ציוד הקלטה רגיש ידרשו אישור מיוחד.

**הנחת MVP:** כל עובד יכול להשאיל כל פריט זמין. קטגוריית הציוד משמשת להצגה ולסינון בלבד. הרשאות לפי קטגוריה, עובד או שווי ציוד הן הרחבה עתידית.

### 2. האם השאלה דורשת אישור מנהל, ומתי הפריט נחשב מושאל?

תהליך אישור מספק יותר שליטה, אך מוסיף שלבים ומאט את השימוש היום־יומי.

**הנחת MVP:** ההשאלה היא Self-Service ואינה דורשת אישור. לחיצה על "השאל פריט" מייצגת גם את הלקיחה הפיזית, ולכן יוצרת מיד השאלה פעילה ומשנה את הפריט ל־`borrowed`.

### 3. כיצד מאמתים שהציוד באמת הוחזר?

ייתכן שדיווח עצמי של העובד אינו מספיק במערכת עתידית.

**הנחת MVP:** העובד מסמן בעצמו החזרה של פריט שנמצא אצלו. מנהל רשאי לסגור השאלה פעילה עבור עובד אחר כאשר הפריט חזר בפועל אך המערכת לא עודכנה. זהו Admin Override ממוקד, לא עריכה חופשית של רשומת ההשאלה.

### 4. האם צריך לשמור היסטוריה או רק את המצב הנוכחי?

היסטוריה מאפשרת לבדוק מי החזיק פריט ומתי, גם לאחר שהוחזר.

**הנחת MVP:** כל השאלה נשמרת כרשומת `Loan` נפרדת גם לאחר סיומה. הדבר מספק traceability בסיסי, אך אינו Audit Trail מלא: שינויי זמינות ופעולות מנהל אינם נשמרים כאירועים נפרדים. הממשק מתמקד בהשאלות הפעילות ואינו כולל מסך היסטוריה מלא.

### 5. האם עובדים יכולים לשריין ציוד מראש?

**הנחת MVP:** אין שריון עתידי. ניתן להשאיל רק פריט שזמין ברגע הפעולה. תאריכי הזמנה, התנגשויות, ביטולים והתראות הם הרחבה עתידית.

### שאלות נוספות שנבחנו

**כיצד מזוהה העובד הפעיל?**  
אין Authentication אמיתי. מתג "עובד / מנהל" מדמה הרשאות לצורכי הדגמה, והמשתמש הפעיל במצב עובד הוא בר משה. במערכת אמיתית הזהות והתפקיד יתקבלו ממערכת ההזדהות הארגונית.

**מה קורה אם שני עובדים מנסים להשאיל את אותו פריט כמעט בו־זמנית?**  
הפעולה בודקת שהפריט עדיין `available` ושאין עבורו השאלה פעילה. רק הפעולה הראשונה אמורה להצליח. ב־Production האכיפה תתבצע בטרנזקציה או ב־constraint אטומי בבסיס הנתונים.

**האם קיימת מגבלת זמן או מדיניות איחור?**  
אין מועד החזרה מחייב. נשמר `borrowedAt` ומוצג הזמן שחלף. השאלה שנמשכת יותר משבעה ימים מודגשת למנהל כ"מושאל זמן רב", אך אינה מוגדרת כ"באיחור" ללא מדיניות עסקית מפורשת.

**האם ניתן להעביר ציוד ישירות מעובד לעובד?**  
לא. יש לסיים את ההשאלה הקיימת ורק לאחר מכן ליצור השאלה חדשה. כך נשמרת אחריות ברורה והיסטוריה רציפה.

**מה קורה אם פריט אבד או ניזוק בזמן השאלה?**  
דיווח אובדן או תקלה אינו חלק מה־MVP. אין סטטוס `lost`, ואין ליצור מעבר ישיר מ־`borrowed` ל־`unavailable`. עד להסדרה תפעולית הפריט נשאר מושאל, כדי שלא לתעד החזרה שלא התרחשה. בגרסה עתידית נכון להוסיף תהליך Incident ייעודי ולא להעמיס משמעות נוספת על סטטוס ההשאלה.

**מי רשאי להוסיף ציוד חדש למלאי?**  
מנהל בלבד. הוא מזין שם, מספר נכס ייחודי, קטגוריה וסטטוס התחלתי. ניתן ליצור פריט כ־`available` או `unavailable`, אך לא כ־`borrowed`, משום שסטטוס מושאל מחייב רשומת `Loan` פעילה.

---

## 2. מודל נתונים

### User

מייצג עובד במערכת.

- `id`
- `name`

התפקיד אינו נשמר על המשתמש ב־MVP; מתג התפקידים מדמה את ההרשאה הפעילה.

### EquipmentItem

מייצג פריט ציוד פיזי.

- `id`
- `assetTag` — מזהה עסקי ייחודי, לדוגמה `KES-LT-021`
- `name`
- `category` — `laptop` | `monitor` | `projector` | `recording`
- `status` — `available` | `borrowed` | `unavailable`

אין לשמור בתוך הפריט את שם העובד או את מועד ההשאלה; מידע זה שייך לרשומת `Loan`.

### Loan

מייצג השאלה אחת של פריט לעובד.

- `id`
- `equipmentId`
- `employeeId`
- `borrowedAt`
- `returnedAt` — ריק כל עוד ההשאלה פעילה
- `status` — `active` | `returned`

### קשרים ואילוצים

- `User 1:N Loan`
- `EquipmentItem 1:N Loan`
- בכל רגע לפריט יכולה להיות לכל היותר השאלה פעילה אחת.
- פריט במצב `borrowed` חייב להיות מקושר להשאלה פעילה אחת.
- פריט במצב `available` או `unavailable` אינו יכול להיות מקושר להשאלה פעילה.
- `assetTag` הוא ייחודי ללא תלות באותיות גדולות או קטנות.

---

## 3. מכונת מצבים

### EquipmentItem

| מעבר | משמעות | מי רשאי לבצע |
| --- | --- | --- |
| יצירה → `available` | הוספת פריט שניתן להשאלה | מנהל |
| יצירה → `unavailable` | הוספת פריט שעדיין אינו בשימוש | מנהל |
| `available → borrowed` | יצירת השאלה פעילה | עובד, עבור עצמו |
| `borrowed → available` | סגירת ההשאלה הפעילה | העובד המחזיק או מנהל |
| `available → unavailable` | הוצאת פריט זמין משימוש | מנהל |
| `unavailable → available` | החזרת פריט למלאי הזמין | מנהל |

אין מעבר ישיר `borrowed → unavailable`. תחילה יש לפתור או לסיים את ההשאלה הפעילה בלי לפגוע באמינות ההיסטוריה.

### Loan

- `active → returned` בלבד.
- בעת המעבר נשמר `returnedAt` והפריט חוזר ל־`available`.
- השאלה שהסתיימה אינה נפתחת מחדש; השאלה עתידית של אותו פריט נוצרת כרשומה חדשה.

שינוי סטטוס הפריט ורשומת ההשאלה הוא פעולה עסקית אחת. ב־Production יש לבצע אותה באופן אטומי.

---

## 4. הרשאות

| פעולה | עובד | מנהל |
| --- | --- | --- |
| צפייה בציוד ובזמינות | כן | כן |
| השאלת פריט זמין | כן, לעצמו | לא במצב מנהל |
| החזרת פריט | רק פריט שנמצא אצלו | כל השאלה פעילה |
| צפייה בכלל ההשאלות הפעילות | לא | כן |
| צפייה במי מחזיק פריט וממתי | לא עבור עובדים אחרים | כן |
| צפייה במסך היסטוריה מלא | לא קיים ב־MVP | לא קיים ב־MVP |
| סימון `available` כ־`unavailable` | לא | כן |
| סימון `unavailable` כ־`available` | לא | כן |
| הוספת פריט ציוד | לא | כן |
| עריכת פריט או מחיקתו | לא קיים ב־MVP | לא קיים ב־MVP |

---

## 5. כללי יצירת ציוד

- שם הפריט ומספר הנכס הם שדות חובה לאחר הסרת רווחים מיותרים.
- מספר הנכס מנורמל לאותיות גדולות ונבדק כייחודי.
- הסטטוס ההתחלתי הוא `available` כברירת מחדל; מנהל יכול לבחור `unavailable`.
- לא ניתן ליצור פריט במצב `borrowed` ללא השאלה פעילה.
- פריט חדש מתווסף מיד לנתוני המלאי, לסטטיסטיקות ולחיפוש.
- עריכה ומחיקה של פריטים אינן חלק מה־MVP.

---

## 6. משוב ומקרי קצה

כל פעולה מציגה למשתמש משוב ברור בעברית. המערכת מטפלת לפחות במקרים הבאים:

- ניסיון להשאיל פריט שאינו זמין.
- ניסיון ליצור השאלה נוספת לפריט שכבר מושאל.
- ניסיון להחזיר פריט ללא השאלה פעילה.
- עובד שמנסה להחזיר פריט של עובד אחר.
- Manager Override לסגירת השאלה פעילה.
- ניסיון לשנות פריט מושאל ישירות ל־`unavailable`.
- יצירת פריט עם מספר נכס חסר או כפול.
- חיפוש שאינו מחזיר תוצאות.

---

## 7. מחוץ להיקף והמשך ל־Production

לא נכללים ב־MVP: Authentication אמיתי, Database, שריונים, התראות, QR, עריכה ומחיקה של מלאי, דיווח אובדן או תקלה, מסך היסטוריה מלא ו־Audit Trail קבוע.

במערכת Production נדרשים persistence, הרשאות בצד השרת, טרנזקציות למניעת השאלה כפולה, audit trail, בדיקות אוטומטיות ותהליך ייעודי לטיפול באובדן ובתקלות.

## עיקרון מנחה

בכל רגע המערכת צריכה לענות בבירור על שלוש שאלות: **מה מצב הפריט, מי אחראי עליו כרגע, וממתי.** המידע ההיסטורי אינו נדרס, כדי שניתן יהיה להבין גם מה קרה לפריט לאורך זמן ולא רק את מצבו הנוכחי.
